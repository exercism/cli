package config

import "testing"

func TestNormalizeWorkspace(t *testing.T) {
	// I don't have access to a windows machine,
	// so I am guaranteed to get this wrong.
	t.Fatal("Need to implement normalization tests for windows.")
}
